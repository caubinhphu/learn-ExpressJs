h2 Users list
form(action="/users/search", method="GET")
	input(type="text" name="q" value=`${query}` === 'undefined' ? '': `${query}`)
	button Search
ul
	each use in users
		li= use.name + ' ' +use.age